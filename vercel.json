{
  "version": 2,
  "builds": [
    {
      "src": "main.go", // 指定构建入口文件
      "use": "@vercel/go" // 使用 Go 构建器
    }
  ],
  "routes": [
    {
      "src": "/(.*)", // 路由匹配模式，匹配所有请求
      "dest": "main.go" // 路由目标，指定为构建入口文件
    }
  ]
}
